{
  "development": {
    "postgresql": {
      "host": "localhost",
      "database": "{{name}}_development",
      "user": "{{name}}",
      "password": "!development"
    }
  },
  "test": {
    "postgresql": {
      "host": "localhost",
      "database": "{{name}}_test",
      "user": "{{Name}}",
      "password": "!test"
    }
  },
  "production": {
    "postgresql": {
      "host": "localhost",
      "database": "{{name}}_production",
      "user": "{{name}}",
      "password": "!production"
    }
  }
}
