// Generated by CoffeeScript 1.6.3
(function() {
  var pty, spawn;

  spawn = require('child_process').spawn;

  pty = require("pty.js");

  exports.spawn = function(command, args, opts, cb) {
    var cmd;
    cmd = spawn(command, args, opts);
    cmd.stdout.on('data', function(data) {
      return console.log(data.toString());
    });
    cmd.stderr.on('data', function(data) {
      return console.error(data.toString());
    });
    return cmd.on('close', function(code) {
      return cb(code);
    });
  };

  exports.launch = function(command, args, opts) {
    var cmd;
    cmd = spawn(command, args, opts);
    return cmd.unref();
  };

  exports.ptyspawn = function(command, args, opts, cb) {
    var cmd;
    cmd = pty.spawn(command, args, opts);
    return cb();
  };

}).call(this);
